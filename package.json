{
  "name": "atlas",
  "private": true,
  "scripts": {
    "build": "hugo",
    "build:preview": "hugo --buildFuture",
    "start": "hugo server",
    "server": "hugo server",
    "server:with-drafts": "hugo server --buildFuture"
  },
  "dependencies": {
    "autoprefixer": "^9.0.0",
    "netlify-lambda": "^0.4.0",
    "postcss-cli": "^6.0.0"
  }
}
